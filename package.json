{
  "name": "cook",
  "version": "1.0.0",
  "private": true,
  "description": "Личный проект «{{projectTitle}}»",
  "repository": {
    "type": "git",
    "url": "{{sshUrl}}"
  },
  "bugs": {
    "url": "{{bugsUrl}}"
  },
  "devDependencies": {
    "browser-sync": "2.26.14",
    "eslint": "7.18.0"
  },
  "license": "MIT",
  "scripts": {
    "start": "browser-sync start --server --no-ui --files \"js/**/*.js\"",
    "test": "eslint js/"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "howler": "^2.2.3"
  }
}
